Before running the python program snippet please make sure

1) python 2.7.16 installed with IDLE.
2) Create a folde NLP and copy these two file
3) test1.txt servers as input file.
4) Try to install all the dependency shown in the pip_dependency_installed_list
5) eg pip install nltk==3.4.5

After the above steps are done
1) Open the file using IDLE python tools and do a run module

output:
It will crreate output in (Person and organisation)noun.txt, country.txt(Country) and temp.text for corrected String Bites. 